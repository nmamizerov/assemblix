"""Reusable test data builders (workflow graphs, canned LLM responses).

These are plain helpers, not pytest fixtures — import them where useful:

    from tests.fixtures.workflows import linear_agent_workflow
    from tests.fixtures.llm_responses import text_response, tool_call_response
"""
