# LiveKit media server for voice-agent avatar calls (single node, no Redis).
# Keys come from LIVEKIT_KEYS ("key: secret" — the space matters).
port: 7880
rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true
room:
  empty_timeout: 60
logging:
  level: info
